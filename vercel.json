{
  "name": "ngo-volunteer-platform",
  "version": 2,
  "buildCommand": "npm install && npm install --prefix client && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install --legacy-peer-deps && npm install --prefix client --legacy-peer-deps"
}
